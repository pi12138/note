# 位置参数和关键字参数

def show(p1, p2, p3):
	print('p1:', p1)
	print("p2:", p2)
	print("p3:", p3)


# 函数调用方式
show(1, 2, 3)
print("*" * 30)
# show(p1=1, 2, 3)		# 会报错
show(1,2,p3=3)			# 不会报错，关键字参数应该放在位置参数之后
# show(1,3,p2=2)		# 也会报错，位置参数的值应该对应